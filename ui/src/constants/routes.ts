const routes = {
  login: "/login",
  main: "/",
  appointment: "/appointment",
  contact: "/contact",
  instructor: "/instructor",
  user: "/user",
  room: "/room",
} as const;

export default routes;
